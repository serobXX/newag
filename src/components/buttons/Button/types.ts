export enum ButtonTypes {
  button = 'button',
  reset = 'reset',
  submit = 'submit',
}

export enum ButtonVariants {
  primary = 'primary',
  secondary = 'secondary',
  tertiary = 'tertiary',
  quaternary = 'quaternary',
}
